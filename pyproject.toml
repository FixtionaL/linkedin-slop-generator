[project]
name = "linkedin-slop-generator"
version = "0.1.0"
description = "A Text to LinkedIn slop convertor"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "clipboard>=0.0.4",
    "groq>=1.1.1",
    "st-copy-to-clipboard>=0.1.6",
    "streamlit>=1.55.0",
]
